Formula change list for table formula refresh issue

1) js/custom-table.js line ~3025
Change parser creation logic in injected window.enableFormulaEditing:
- Do not reuse old global parser handlers.
- Recreate parser each call and then register functions.

What to change:
From:
let parser = window.globalFormulaParser;
if (!parser) {
parser = new window.formulaParser.Parser();
window.globalFormulaParser = parser;
...
}

To:
const parser = new window.formulaParser.Parser();
window.globalFormulaParser = parser;
... (keep same setFunction registrations below, but no outer if (!parser) block)



2) js/custom-table.js line ~3067
In injected parser callCellValue:
Add null guard after table lookup:
if (!tableElem) { done(null); return; }

What to add right after tableElem assignment:
if (!tableElem) { done(null); return; }

3) js/custom-table.js line ~3087
In injected parser callRangeValue:
Add null guard after table lookup:
if (!tableElem) { done([]); return; }

What to add right after tableElem assignment:
if (!tableElem) { done([]); return; }



4) js/custom-table.js line ~3552
Change main enableFormulaEditing parser logic:
- Do not reuse stale parser with stacked listeners.
- Recreate parser each call and register functions again.

What to change:
From:
let parser = iframeWindow.globalFormulaParser;
if (!parser) {
parser = new iframeWindow.formulaParser.Parser();
iframeWindow.globalFormulaParser = parser;
...
}

To:
const parser = new iframeWindow.formulaParser.Parser();
iframeWindow.globalFormulaParser = parser;
... (keep same setFunction registrations below, remove outer if (!parser) block)



5) js/custom-table.js line ~3601
In main parser callCellValue:
Add null guard after table lookup:
if (!tableElem) { done(null); return; }

What to add right after tableElem assignment:
if (!tableElem) { done(null); return; }

6) js/custom-table.js line ~3622
In main parser callRangeValue:
Add null guard after table lookup:
if (!tableElem) { done([]); return; }

What to add right after tableElem assignment:
if (!tableElem) { done([]); return; }


7) Recommended extra stability fix ( Not required dont apply )
js/custom-table.js line ~3657
Before adding focus/blur/keydown listeners:
if (cell.hasAttribute('data-formula-enabled')) return;
This prevents duplicate listener stacking.

What to add as first line inside forEach(cell => { ... }):
if (cell.hasAttribute('data-formula-enabled')) return;